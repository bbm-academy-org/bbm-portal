import { MemberCreateScreen } from '../MemberCreateScreen'

export const metadata = { title: 'Новый участник · Админка BBM' }

export default function CreateMemberPage() {
  return <MemberCreateScreen />
}
