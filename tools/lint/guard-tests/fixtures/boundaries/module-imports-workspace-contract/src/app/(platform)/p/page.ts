// LEGAL (spec 311 EARS-457): the (platform) route group renders the registry —
// that is what the composition root is FOR.
import { WORKSPACE_REGISTRY } from '../../../lib/workspace/registry'

export const page = WORKSPACE_REGISTRY
