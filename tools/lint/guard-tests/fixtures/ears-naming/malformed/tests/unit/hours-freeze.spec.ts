// Fixture tree for `ears-naming` — NOT repo content. A malformed EARS prefix.
import { describe, expect, it } from 'vitest'

describe('period freeze', () => {
  it('ears-2: freezes the period', () => {
    expect(true).toBe(true)
  })

  it('rounds the hourly rate', () => {
    expect(true).toBe(true)
  })
})
