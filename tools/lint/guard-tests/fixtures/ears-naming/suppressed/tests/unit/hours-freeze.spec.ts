// Fixture tree for `ears-naming` — NOT repo content.
// ears-naming-ok: this file documents the legacy prefix on purpose
import { describe, expect, it } from 'vitest'

describe('period freeze', () => {
  it('ears-2: freezes the period', () => {
    expect(true).toBe(true)
  })
})
