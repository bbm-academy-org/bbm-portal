// Fixture tree for `ears-naming` — NOT repo content. Canonical prefixes only.
import { describe, expect, it } from 'vitest'

describe('period freeze', () => {
  it('EARS-2: freezes the period', () => {
    expect(true).toBe(true)
  })

  it('EARS-3.1 (#157): emits the audit row', () => {
    expect(true).toBe(true)
  })

  it('rounds the hourly rate', () => {
    expect(true).toBe(true)
  })
})
